/* eslint-disable no-unused-vars */


const initialState = {
  //user
  user:null  
};

const userReducer = (state = initialState, action) => {
  switch (action.type) {    
    default:
      return { ...state };
  }
};

export default userReducer;
